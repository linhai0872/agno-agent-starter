# Agno 框架（含 PostgreSQL、MCP 和 AgentOS 支持）
agno[postgres,mcp]>=1.0.0

# ===========================================
# 模型厂商 SDK
# ===========================================

# OpenAI (GPT-4o, o1/o3 系列)
openai>=1.0.0

# Google Gemini (Agno 原生支持)
google-genai>=1.0.0

# Anthropic Claude (Agno 原生支持)
anthropic>=0.30.0

# 阿里云 DashScope (Qwen 系列)
dashscope>=1.20.0

# 火山方舟 (豆包 Seed, DeepSeek)
volcengine-python-sdk[ark]>=1.0.0

# LiteLLM (统一网关)
litellm>=1.50.0

# ===========================================
# Web 框架和工具
# ===========================================

# Web 框架（AgentOS 依赖）
fastapi>=0.115.0
uvicorn>=0.32.0

# HTTP 客户端
httpx>=0.27.0

# 数据验证
pydantic>=2.0.0
pydantic-settings>=2.0.0

# YAML 配置加载
pyyaml>=6.0

# 环境变量
python-dotenv>=1.0.0

# ===========================================
# 数据库
# ===========================================

# PostgreSQL 数据库（异步支持）
psycopg[binary,pool]>=3.1.0
sqlalchemy>=2.0.0

# 向量数据库（知识库支持）
pgvector>=0.2.0

# ===========================================
# 外部工具
# ===========================================

# 搜索工具 (Tavily - Agno 原生支持)
tavily-python>=0.5.0

# 搜索工具 (DuckDuckGo - Agno 依赖 ddgs)
ddgs>=7.0.0

# ===========================================
# Tracing / Observability
# ===========================================

# OpenTelemetry (Agno Tracing 依赖)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
openinference-instrumentation-agno>=0.1.0
